// CreateQuoteDto — validated shape for creating a quote record.
// Extend with class-validator decorators (@IsString, @IsInt, etc.) during Phase 1 implementation.
export class CreateQuoteDto {
  [key: string]: unknown;
}
