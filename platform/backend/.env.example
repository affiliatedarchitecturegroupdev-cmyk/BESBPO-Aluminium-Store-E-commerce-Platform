DATABASE_URL="postgresql://user:password@localhost:5432/aluminium_store?schema=public"
JWT_SECRET="replace-with-a-real-secret"
PRICING_SERVICE_URL="http://localhost:8000"
REDIS_URL="redis://localhost:6379"
# Only needed when seeding with NODE_ENV=production. The development defaults used otherwise are
# published in this repository, so the seed refuses to run in production without these.
SEED_ADMIN_PASSWORD=""
SEED_TRADE_PASSWORD=""
PAYFAST_MERCHANT_ID=""
PAYFAST_MERCHANT_KEY=""
LULAPAY_API_KEY=""
PAYJUSTNOW_API_KEY=""
PORT=4000
